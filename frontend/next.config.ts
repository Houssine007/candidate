import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  experimental: {
    // reactCompiler: true, // Désactivé pour l'instant pour éviter les warnings
  }
};

export default nextConfig;
